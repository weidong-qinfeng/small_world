# 会话恢复上下文（Session Context）

> 用途：新会话（或清理上下文后）快速恢复项目状态。让新会话**先读本文件**再工作。
> 最后更新：M3 验收通过时（M0→M3 已完成；M4 清单待产出）。

---

## 一、项目全景（两条线）

| 线 | 目录 | 状态 | 说明 |
|---|---|---|---|
| **神经仿真（当前主线）** | `neural_exploration/` | M0 ✅ M1 ✅ M2 ✅ | 自底向上复刻真实生物神经系统 |
| **符号认知层（旧线/旁线）** | `digital_brain/` | M1-M3 已完成，已推送 | 知识图谱+激活扩散+程序性记忆，小学应用题 |

两者相互独立；神经仿真的 M7 计划把生物机制回迁数字大脑。

---

## 二、神经仿真：里程碑进度

| 里程碑 | 状态 | 交付物 |
|---|---|---|
| M0 工具链与环境 | ✅ 已验收 | venv + 引擎基准（Brian2 主线）+ 最小闭环 4 测试 |
| M1 多隔室 HH 神经元 | ✅ 已验收（P1-P8 全达标） | 18 隔室形态 + 5 项生理验证 + 14/14 测试 |
| M2 突触与神经元对 | ✅ 已验收（P1–P7 全达标） | 化学突触(AMPA/NMDA/GABA)+电突触+STP；25/25 测试 |
| M3 触觉反射弧 | ✅ 已验收（P1–P7 全达标） | ReflexArc 多神经元链(PLM→AVM→DA/VB)+肌肉双通道；P3/P4/P5 按主agent裁决修订判据；32/32 测试 |
| M4 嗅觉趋化回路 | 📋 清单待产出 | 交接点已就绪（ReflexArc 环境→感觉接口扩展，~20 神经元） |
| M5 全连接组 302 神经元 | 待 | 核心里程碑 |
| M6 学习与可塑性 | 待 | — |
| M7 扩展/回迁 | 待 | — |
| **阶段二**（M8–M14） | 📋 设计文档已审阅通过 | `docs/生物仿真实验设计-从行为到大脑神经网络.md`（果蝇→斑马鱼→小鼠→灵长类） |

**角色分工（重要）**：主规划节点 session **只做规划与验收，不做执行**。
执行由独立执行节点按清单实施；完成后回到规划节点验收（复跑测试、核对 Pass、查产物真实性）。

**开发 workflow（新增）**：`docs/开发WORKFLOW.md` 为唯一流程纲领——每里程碑 6 步循环
（交接确认 → 子agent A 产出清单 → 主agent 评审清单 → 子agent B 实施 → 主agent 验收 → git 提交+交接），
验收三态：✅通过 / ❌反证（记缺失机制清单）/ ↩️打回（仅文档不完整）。

M3 已验收（P1–P7 全达标，32/32 测试）；M3 关键裁决：
- P3：行为潜伏期窗 [25,60] 结构性不可达（δ 肌肉瞬跳+无转导）→ 改为回路生理窗 [5,20]ms + 强度单调；
  Chalfie 30–50ms 差异归因登记为 M3 简化假设（转导/肌肉动力学缺失 → M5 补齐）
- P4：单发放幅度恒等 → 潜伏期单调主判据 + 8×I0/20ms 多发放补充验证
- P5：×1.2 均值子判据结构性不可达（VB 张力基线振荡主导）→ 判据落地为「发放数+1 且响应分量符号」
M4 交接：ReflexArc 环境→感觉接口 + ASE ON/OFF 细胞对 + 趋化运动雏形；CI 0.3–0.7 对照（m3_report §6）。

---

## 三、环境与复现命令

```bash
# 环境（Python 3.9.6 venv，依赖锁定）
source .venv-neuro/bin/activate
# 或直接 .venv-neuro/bin/python ...

# 全量测试（M0 4 + M1 10 + M2 11 = 25；~1-6 分钟，冷缓存首次编译更久）
.venv-neuro/bin/python -m pytest neural_exploration/tests

# M1 验证复现（P2-P6 全跑）
.venv-neuro/bin/python -m neural_exploration.tools.run_m1_validation

# M2 验证复现（P1-P5 全跑）
.venv-neuro/bin/python -m neural_exploration.tools.run_m2_validation
```

**环境关键版本**：brian2 2.6.0（主线引擎）、NEURON 9.0.1（参考解）、numpy 1.26.4、
cython 0.29.37（brian2 需 <3.x）、Python 3.9.6（接近 EOL，py3.11 迁移决策点推迟到 M5）。

---

## 四、关键技术约定（踩坑记录，执行节点必读）

1. **Brian2 Im 为"内向正"约定**——用"外向正"写法会导致发散（M1 实测踩坑）
2. **NEURON 温度必须 6.3°C**（Q10 缩放，否则波形偏差）
3. **HH 静息瞬态漂移污染 PSP 基线**——用本地基线（非全局基线）解决
4. **HH 神经元统一加 refractory=2ms**（否则每个时间步都发尖峰）
5. **Brian2 多隔室**：SpatialNeuron/Soma/Cylinder 可用（M1 已验证）
6. **NEURON 点过程电流按 nA 注入**：mod 里 i (pA) 无单位换算会放大 1000×（M2 踩坑，NMDA 假性发放）
7. **NEURON Python 点过程会被 GC 回收**：需保留引用，否则 50Hz 训练只剩最后一脉冲（M2）
8. **Brian2 编译缓存**：TimedArray 形状/名字与 Synapses 参数进入代码串，变化触发 80–120s 重编译——固定形状+显式命名+namespace 传参（M2，也修复了 M1 同类问题）
9. **NEURON 9.0.1 不支持经典 gap.mod 的 EXTERNAL vother**——缝隙连接参考改用 scipy solve_ivp 独立解（M2）
10. **M3 注意**：反射弧多神经元链复用 ChemicalSynapse/GapJunction，注意突触前触发事件与多隔室耦合

---

## 五、当前待办

1. **M4 清单**：由规划节点产出（嗅觉趋化回路——ASE ON/OFF 细胞对 → AIY/AIB → 运动，~20 神经元；交接点已就绪）
2. **M4 执行**：按 M4 清单实施（执行节点）
3. **M4 验收**：完成后回规划节点复跑测试、核对 Pass
4. **git**：每个里程碑独立提交（沿 M0/M1/M2/M3 惯例），推送 origin/main

---

## 六、数字大脑线（旁线，暂停中）

- 已完成：M1 表层鲁棒性、M2 语义栈、M3 SRL+事件FSM（117 测试全绿）
- 已知边界：符号派"修不完"（组合爆炸），已出备选方案
  `neural_exploration/docs/混合架构设计-统计理解层.md`（路线 B，暂缓）
- 用户已转向神经仿真路线（本目录主线）
